{
  "name": "expense_tracker",
  "version": "1.0.0",
  "description": "A simple app to analyse and track your daily spendings. Backend currently on MongoDB",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prefix": "^1.0.0"
  },
  "devDependencies": {}
}